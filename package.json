{
  "name": "web-observe",
  "version": "1.0.0",
  "private": true,
  "description": "Semantic normalizer that turns browser observations into causal, source-linked explanations for AI coding agents",
  "bin": { "web-observe": "src/observe.js" },
  "scripts": {
    "test": "node --test test/",
    "observe": "node src/observe.js"
  },
  "dependencies": {
    "playwright": "^1.45.0"
  }
}
